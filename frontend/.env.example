# MediShield AI - Frontend Environment Variables Template

# URL of the deployed backend server API endpoint (include '/api' suffix)
VITE_API_URL=http://localhost:5000/api

# OCR.space API Key for client-side scanning (optional, falls back to demo mode if blank)
VITE_OCR_API_KEY=your_ocr_api_key_here
